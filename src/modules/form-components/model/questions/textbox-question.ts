import { FormQuestion } from './form-question';

export class TextboxQuestion extends FormQuestion<string> {
  controlType = 'textbox';
  type: string;

  constructor(options: {} = {}) {
    super(options);
    this.type = options['type'] || '';
  }
}
